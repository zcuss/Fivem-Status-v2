{
  "name": "fivem-status",
  "version": "2.0.0",
  "private": true,
  "scripts": {
    "setup": "cd backend && pnpm install && pnpm build && cd ../frontend && pnpm install && pnpm build",
    "start:backend": "cd backend && pnpm start:api",
    "start:frontend": "cd frontend && pnpm start"
  },
  "engines": {
    "node": ">=20"
  }
}
